import React from "react";

const Sample = () => {
  return (
    <>
      <div>Sample</div>
      <button onClick={() => console.log("buttonだよ")}>ボタン </button>
    </>
  );
};

export default Sample;
